# encontre mais de uma forma de mostrar na tela o número de linhas
# em que a palavra Unix aparece pelo menos uma vez no arquivo atividade
# (não mostrar as linhas)

grep -n "Unix" atividade | cut -d: -f1

grep -n "Unix" atividade | sed 's/[^0-9]*\([0-9]*\).*/\1/'


